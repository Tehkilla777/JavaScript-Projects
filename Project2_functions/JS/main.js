function myFunction() {
    var words = "Hello!"; //The variable
    words += " How are you?"; //+= operator adds to the variable
    document.getElementById("Concatenate").innerHTML = words; //uses document.getElementById()
}